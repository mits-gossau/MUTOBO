@using Dit.Umb.ToolBox.Models.Interfaces
@using Dit.Umb.ToolBox.Models.PoCo
@model  Dit.Umb.ToolBox.Models.Interfaces.ISliderComponent

<third-party-macro-carousel pagination loop height="@(Model.Height != null ? $"{Model.Height}px" : "auto")"  namespace="carousel-" interval="@(Model.Interval ?? 5000)">
    @foreach (var media in Model.Slides)
    {
        if (media is Dit.Umb.ToolBox.Models.PoCo.Image image)
        {
            @image.RenderWcPictureTag(nameSpace: Model.GetPictureNameSpace(), useSources: true)
        }
        else if (media is Dit.Umb.ToolBox.Models.PoCo.Picture picture)
        {
            @picture.Image.RenderWcPictureTag(nameSpace: Model.GetPictureNameSpace(), useSources: true)
        }
        else if (media is IVideoComponent video)
        {
        
                @Html.Partial("~/Views/Partials/VideoComponent.cshtml", video)
    

        }
 
    }
</third-party-macro-carousel>